## Layer management

- Move types.ts to models directory
- Move opacity slider to color & tone tool in adjustments
- Order of layers on display is inverted
- Can select layers through another one
    - Allow if the pixels in the top layer are transparent?
- Ability to select multiple layers with ctrl
- Move transformer to Layer component
- Check behavior of throwing error in findLayer for example
- Rename prevImages to prevLayers
-

## Undo/redo


- Can move layers sometimes, should not be able to
- Can select layers when clicking outside of it
- Selection can group layers somehow, the transformer contains both images
- Virutal
